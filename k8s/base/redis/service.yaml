apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  type: LoadBalancer
  ports:
  - port: 6379
  selector:
    pod: redis

---

apiVersion: v1
kind: Service
metadata:
  name: redis-replica
spec:
  type: LoadBalancer
  ports:
  - port: 6379
  selector:
    pod: redis-replica
